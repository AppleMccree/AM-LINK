# AM-LINK 待办需求

## 课程较多时的查看与管理

- 当前问题：课程逐渐增多后，全部平铺在左侧边栏会过长、难以查找。
- 后续增加独立的“查看全部课程”入口，不挤占当前课堂区域。
- 全部课程页面支持搜索、滚动浏览、最近使用排序和归档课程筛选。
- 左侧边栏只显示最近/常用课程，并提供“查看全部”按钮。
- 课程内仍保持“课程 → 第 1 节课、第 2 节课……”的层级，不把课堂记录与课程混在一起。
- 本轮只记录需求，暂不修改应用。

## 实时字幕选择提示偶发遮挡

- 当前问题：实时字幕区域在选中文字、右键复制或“问 AI”提示出现时，偶尔会显示白色浮动提示框；该框位置异常，会遮挡大字号字幕内容。
- 后续修复：统一字幕选择提示、右键菜单与 ToolTip 的深色主题；限制提示层在字幕容器内的正确位置显示，避免覆盖正文；检查焦点切换、文本选择和滚动时的残留提示。
- 本轮只记录需求，暂不修改应用。

## 结束课堂与问 AI 时界面卡住

- 当前问题：结束课堂的收尾阶段，以及点击“问 AI”请求答案时，界面偶发卡住；按钮会长期保持禁用或“处理中”状态。截图中结束后“开始课堂同传”仍呈灰色，学习包/重新总结等操作也可能无法继续。
- 后续修复：梳理结束课堂、学习包生成、问 AI 请求的异步任务和取消令牌；为网络请求增加明确超时与可见的取消/重试状态；无论成功、失败、取消或窗口关闭，都在 `finally` 恢复按钮、窗口输入和课堂状态；避免在 UI 线程等待网络、文件写入或学习包生成。
- 本轮只记录需求，暂不修改应用。

## 双向同传将对方日语误写入“我方中文”一侧

- 当前问题：用户未说话时，对方日语仍会被错误写进左侧“我说中文 → 给对方日文”；截图中状态栏已显示“对方多人 · 日文 → 中文”，但左侧仍残留日语输出，说明自动分流、历史落盘或状态显示之间存在不一致。
- 后续修复：为每个最终识别段建立唯一的“语言/说话人/目标栏位”决策并在写入前校验；对方日语一旦已确认，禁止同一识别段进入左栏；将音色画像只作为辅助证据，不能覆盖明确的日语文字/ASR证据；增加路由审计日志和“上一句分错了”后的完整回滚校验；对未说话的我方音色不得自动学习或生成左栏记录。
- 本轮只记录需求，暂不修改应用。

## 字幕视图偶发自动跳回前面

- 当前问题：正常课堂与双向同传的字幕/记录区域，用户正在浏览内容时，视图偶尔会被程序自动拉回前面，打断阅读位置。
- 后续修复：统一两个字幕界面的滚动锚点与追加策略；只有用户原本停留在底部时才自动跟随最新字幕；用户翻看前文时保留精确滚动位置，并显示“有新字幕 · 回到最新”按钮；排查异步刷新、TextBox 重设文本和历史重载导致的滚动重置。
- 本轮只记录需求，暂不修改应用。

## 同传中自由回看与原文查看

- 当前需求：同传持续进行时，学生必须能自由滚动查看前面的字幕、问题和上下文；新字幕不得把视图强行带回最新位置。
- 当前需求：同传界面需要保留原文查看能力。为避免把中文和外语挤在给对方阅读的同一栏，原文应作为可展开/可切换的辅助区域，或按条目展开查看，而非默认混排在目标译文中。
- 后续实现：实时与历史字幕均保存原文、译文和时间戳；增加“显示原文”开关或单条展开入口；回看时保持滚动位置，并允许复制原文、译文或两者；新字幕到来只显示“回到最新”提示。
- 本轮只记录需求，暂不修改应用。

## 实时同传偶发停在临时字幕

- 当前问题：课堂同传偶发停在一段“临时”识别/翻译结果，进度条仍有活动痕迹，但后续确认字幕或状态没有继续推进。截图中临时结果为重复口头语（如“嗯、好嘞、还在啊、对、所以这个”），且被以大字号长期留在主显示区。
- 后续修复：区分 ASR 临时段、最终段和网络重连状态；为临时段增加最大显示时长与失效清理，识别流停滞时主动取消并从最近音频缓冲重连；最终段未抵达时不得把临时口头语长期冻结为主字幕。重连后应保留已确认字幕，不重复写入。
- 本轮只记录需求，暂不修改应用。

## 高优先级：结束课堂反复卡死与实时字幕闪烁

- 当前问题：用户已多次复现：点击“停止课堂”后应用会整体卡住，必须重启才能继续使用；同一时段实时字幕区域会持续闪烁。这不是偶发体验问题，需按高优先级稳定性故障处理。
- 初步关联：停止流程可能仍与 ASR/翻译临时任务、音频泵、课堂文件落盘或学习包请求并发争用 UI 线程；字幕闪烁可能来自临时字幕、确认字幕、清空状态和滚动更新相互覆盖。
- 后续修复：将停止课堂拆分为“立即停止采集并恢复界面”与“后台保存/生成学习包”；所有异步任务使用独立取消令牌、超时和 finally 清理。字幕以段落状态机更新，禁止临时/确认/空状态在同一渲染周期互相覆写，并对 UI 更新去抖。
- 本轮只记录需求，暂不修改应用。

## 同传双行显示、后台总结与投屏 PPT 聚焦

- 同传显示：用户希望不再使用“显示原文”勾选开关。每条确认记录固定为“译文在上（主字号）+ 原文在下（较小、弱化颜色）”，两者一起出现，既方便对话也不把原文与译文混为同一层级。
- 学习总结：点击“重新总结/AI学习包”后不能让界面停住或必须等待完成才能继续操作。应立即显示后台任务状态、允许继续查看记录/开始下一操作；完成后刷新入口并通知，失败可重试。课堂结束时同样不能等待总结完成。
- PPT 与实时翻译布局：当前即使应用最大化，PPT仍受三栏布局和大量黑边限制，组会投屏时文字过小。不要另做固定的“组会布局”或纯 PPT 全屏（会遮住同传）。在 PPT 区与右侧实时翻译区之间增加可拖拽的竖向分隔条；用户先通过现有按钮收起左侧课程栏，再自由拖动两区宽度，现场自行决定 PPT 和翻译各占多少。翻页控件始终保留，可再提供 PPT 缩放控制。
- 本轮只记录需求，暂不修改应用。
